<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Registers CiMode.activator before Application.onCreate completes (qaDebug only). -->
        <provider
            android:name=".ci.CiModeInitProvider"
            android:authorities="${applicationId}.ci-mode-init"
            android:exported="false"
            android:initOrder="100" />
    </application>
</manifest>
