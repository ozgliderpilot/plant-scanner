package com.nursery.core

/**
 * Common discount percentages offered as large quick-pick buttons on Enter price / Edit price.
 * Volunteers can still type any other 0–100 value in the Discount % field.
 */
object DiscountPresets {
    val pcts: List<Int> = listOf(10, 15, 20)
}
