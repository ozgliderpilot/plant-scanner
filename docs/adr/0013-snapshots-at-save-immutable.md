# Snapshots at save; post-save immutable

Taxonomic identity (and related display fields) is snapshotted onto line items and cull records at save time — export never re-looks up the live plant cache. After save, receipts and culls are immutable: no edit or delete in the app (history is display-only).
