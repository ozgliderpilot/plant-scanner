# Money is integer cents; price keyed at sale

All monetary math uses integer cents — never floats. The plant sheet has no price column, so unit price is keyed on the line item at sale; discount is a per-line percentage (0–100). Payment method is recorded on the receipt; the app does not process cash or card.
