# Accessibility-first UI for elderly volunteers

Every screen is designed for elderly nursery volunteers: large tap targets and text, high contrast, no flicker from silent background sync, and tap-not-gesture interactions (no swipe-to-delete). Accessibility is a primary constraint, not a polish pass.
