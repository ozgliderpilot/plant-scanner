appId: com.nursery.scanner.test
---
# Volunteer-critical gallery walk (#72). Text selectors first.
# Launch with CI mode so seed-once + camera placeholder run before the first frame.
# Screenshot paths are relative to the Maestro workspace (repo root), not --output.
- launchApp:
    clearState: true
    arguments:
      com.nursery.scanner.CI_MODE: true
# Cold start + CI seed can leave the launcher visible briefly; wait for Actions UI.
- extendedWaitUntil:
    visible: "Sell plants"
    timeout: 30000
- takeScreenshot:
    path: maestro-out/01-actions
- tapOn: "Plants"
- takeScreenshot:
    path: maestro-out/02-plants
- tapOn: "Actions"
- tapOn: "Sell plants"
- takeScreenshot:
    path: maestro-out/03-sell-scan
- tapOn: "Type number instead"
- tapOn:
    text: "Accession number"
- inputText: "1001"
# Soft keyboard covers Find on Pixel-class AVDs; hideKeyboard uses BACK which only
# dismisses the IME while it is showing (ScanScreen BackHandler does not run).
- hideKeyboard
- tapOn: "Find"
- takeScreenshot:
    path: maestro-out/04-sell-line
- tapOn:
    text: "Unit price ($)"
- inputText: "5.00"
- hideKeyboard
- tapOn: "Add to receipt"
- takeScreenshot:
    path: maestro-out/05-sell-cart
- tapOn: "Finish & save"
# Wait for async finishAndSave → ConfirmScreen (cart stays up until persist completes).
- assertVisible: "Total to collect"
- takeScreenshot:
    path: maestro-out/06-sell-confirm
- tapOn: "Done"
- tapOn: "History"
- tapOn: "View Receipts"
- takeScreenshot:
    path: maestro-out/07-receipts
- back
- tapOn: "View Culled"
- takeScreenshot:
    path: maestro-out/08-culls
